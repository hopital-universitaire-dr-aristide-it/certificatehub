# Point de reprise — 29 août 2026

Écrit avant une bascule de compte Claude. **La liste des conversations est rangée
par compte ; le dépôt, la mémoire et les transcripts ne le sont pas.**

## Ce qu'est CertificateHub

Application indépendante et dockerisée pour la délivrance de certificats
médicaux à l'**Hôpital Universitaire Dr. Aristide** — le Certificat de Santé
d'abord. Hébergée sur un poste du service informatique, jointe par les autres
postes **en réseau local** (IP:port). Dépôt
`hopital-universitaire-dr-aristide-it/certificatehub`.

Pile : Laravel 12 (API) + React/Vite (SPA) + PostgreSQL 16 + Redis + Typesense.

## Où en est le projet

Branche `main`, dernier commit
`1cfb45a feat: add reception "Consulter certificats" page with patient/doctor/date filters`
(29 juillet).

**Le frontend est construit** — la tâche n°8 est finie. Reste **la tâche n°9 :
sauvegarde, TLS, test de fumée.**

> **Attention : du travail non commité est présent, et il correspond
> exactement à la tâche n°9.** À reprendre plutôt qu'à écraser :
>
> ```
>  M docker/nginx/default.conf
>  ?? docker/backup/
>  ?? docker/nginx/default.prod.conf
>  ?? docker/nginx/snippets/
>  ?? scripts/
> ```
>
> **Le lire avant toute commande qui restaure.** `git checkout --` sur un
> dossier a déjà détruit du travail non commité sur cette machine, deux fois.

## Le prochain pas

Terminer la tâche n°9 : la sauvegarde (`docker/backup/`), le TLS
(`docker/nginx/default.prod.conf` et `snippets/`), puis **un test de fumée
réel** — pas un `docker compose ps` vert, mais un certificat émis de bout en
bout depuis un autre poste du réseau.

Le déploiement est décrit dans [`DEPLOYMENT.md`](DEPLOYMENT.md).

## Reprendre

```bash
cd "C:/Users/Ben/Documents/UNIFA/hopital-universitaire-dr-aristide-it/certificatehub"
git status
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
```

Les transcripts sont dans
`~/.claude/projects/C--Users-Ben-Documents-FRST-Capstone-Project/`, indépendants
du compte.
